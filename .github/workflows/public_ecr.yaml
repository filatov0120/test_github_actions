name: TEST 
on:
  workflow_dispatch:

jobs:

  publish-docker:
    name: Publish Docker Image
    runs-on: ubuntu-20.04
    env:
      name: ${GITHUB_REPOSITORY#*/}"
      tag: ${GITHUB_SHA}
      ECR_REGISTRY: public.ecr.aws/i6t0p5x2/r-dee_go_quorum 
      AWS_REGION: eu-central-1
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v3
      - name: Build docker image
        run: |
          docker build . -t public.ecr.aws/i6t0p5x2/r-dee_go_quorum:latest

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Login to Amazon ECR Public
        uses: aws-actions/amazon-ecr-login@v1
        with:
          mask-password: 'true'
          registry-type: public   

      - name: Build, tag, and push docker image to Amazon ECR Public
        env:
          REGISTRY: public.ecr.aws/i6t0p5x2/r-dee_go_quorum
          REGISTRY_ALIAS:
          REPOSITORY: r-dee_go_quorum
          IMAGE_TAG: latest
        run: |
          docker build -t $REGISTRY/$REGISTRY_ALIAS/$REPOSITORY:$IMAGE_TAG .
          docker push $REGISTRY/$REGISTRY_ALIAS/$REPOSITORY:$IMAGE_TAG


      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v1  
      # - name: Push Docker image to ECR
      #   run: |
      #     docker push ${{ env.ECR_REGISTRY }}:${{ env.tag }}
      #     docker push ${{ env.ECR_REGISTRY }}:latest
